# Die App selbst braucht KEINE Pakete – sie ist reines Vanilla-JS, und serve.py
# (statischer Dev-Server) nutzt nur die Python-Standardbibliothek.
#
# Nur das OPTIONALE Backend (server.py: gemeinsame Ablage fuer Modelle und
# Bibliothek, siehe README) braucht ein Paket:
aiohttp>=3.9
